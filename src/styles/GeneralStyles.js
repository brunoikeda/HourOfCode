import { StyleSheet } from "react-native";

const generalStyles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#EAF728',
      alignItems: 'center',
      justifyContent: 'center',
    },
    title: {
      fontSize: 18
    }
  });

export default generalStyles;